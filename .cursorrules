# Cursor AI Rules for WEB2POS Project

## Auto-read README.md on startup
Always read and understand the README.md file first when starting work on this project. The README contains:
- Project overview and technology stack
- Environment setup instructions
- ID generation rules
- Channel types
- Main features including table map management
- Database usage rules
- Development and execution methods

## Project Context
This is a restaurant POS system (WEB2POS) with the following key components:
- Frontend: React + TailwindCSS
- Backend: Node.js + Express
- Database: SQLite
- Features: Menu management, table map management, multi-channel ordering, Firebase integration

## Important Files to Always Consider
- README.md - Project documentation and setup
- idGenerator.js - ID generation rules
- .env - Environment variables
- web2pos.db - SQLite database

## Table Map Features
The table map system includes comprehensive element management with:
- Drag & drop functionality
- Text editing capabilities
- Resize and rotation features
- Multi-floor support
- History management (Undo/Redo)
- Screen size management
- Element type-specific characteristics

Always refer to the README.md for complete feature documentation when working on this project.

## Security and Error Prevention Features
Essential features for system security that must be implemented:

### Input Validation
- Always validate and sanitize user inputs before database operations
- Use parameterized queries to prevent SQL injection
- Validate ID formats (storeId, itemId, orderId, etc.) using idGenerator rules
- Check data types and ranges for numerical inputs (prices, quantities, etc.)

### Error Handling
- Always wrap database operations in try-catch blocks
- Return meaningful error messages to frontend
- Log errors with context (timestamp, user action, data involved)
- Never expose sensitive information in error messages

### Data Integrity
- Verify foreign key relationships before insert/update operations
- Check for duplicate entries (especially for unique IDs)
- Validate business logic (e.g., payment amount vs order total)
- Ensure atomic operations for related data updates (use transactions)

### Authentication & Authorization
- Verify user permissions for sensitive operations (void, refund, etc.)
- Protect admin-only endpoints
- Validate session tokens and employee credentials

### Database Safety
- Always backup before structural changes
- Use transactions for multi-table operations (orders + payments)
- Implement soft deletes where appropriate (keep audit trail)
- Validate data before batch operations

### Financial Data Protection
- Double-check calculations (tax, total, change)
- Validate payment amounts before processing
- Keep payment audit trail (who, when, how much)
- Prevent negative prices or quantities

### State Management
- Validate localStorage data before using
- Sync critical data (orders, payments) with backend
- Handle offline/online state transitions safely
- Prevent data loss during table operations (move/merge)

## Protected Areas (Do NOT edit without explicit approval)
- Split Order logic and state management:
  - `frontend/src/hooks/useOrderManagement.ts`
  - `frontend/src/pages/OrderPage.tsx`

- Table Move/Merge operations (완성됨 - 2025-10-23):
  - `backend/routes/table-operations.js`
  - `frontend/src/pages/SalesPage.tsx` (handleMoveMergeTableClick 함수 및 관련 로직)
  
  **핵심 기능:**
  - 테이블 Move: 주문을 다른 테이블로 이동
  - 테이블 Merge: 두 테이블의 주문 병합
  - 체류 시간 이동/제거
  - localStorage 동기화 (주문 ID, Split Guests, VOID, 결제 정보, 체류 시간)
  - 즉시 UI 상태 업데이트
  - 4가지 시나리오 모두 지원 (주문 유무 조합)

- **QSR Mode (완성됨 - 2026-02-04):**
  - `frontend/src/pages/QsrOrderPage.tsx` - QSR 주문 페이지 전체
  - `frontend/src/pages/QsrPage.tsx` - QSR 대시보드
  
  **⚠️ 수정 전 반드시 사용자 컨펌 필요!**
  - 주문 흐름 (메뉴 선택, 모디파이어, 수량 조절)
  - 결제 처리 (Payment Modal, Split Bill)
  - Order History 기능
  - 카테고리/모디파이어 표시 로직
  - 프린터 연동

- **Printer/Graphic Output (완성됨 - 2026-02-04):**
  - `backend/utils/graphicPrinterUtils.js` - 이미지 출력 렌더링 (Kitchen/Receipt/Bill)
  - `backend/routes/printers.js` - 프린터 API 라우트
  - `backend/config/defaultPrinterLayouts.js` - 프린터 레이아웃 기본값
  
  **출력 폼 종류 (3가지):**
  - `renderKitchenTicketGraphic()` - 주방 티켓
  - `renderReceiptGraphic()` - 영수증
  - `renderBillGraphic()` - 청구서
  
  **프론트엔드 프린트 함수 (중복 존재하지만 안정적으로 동작 중):**
  - `QsrOrderPage.tsx`: printKitchenOrders(), handlePrintBill(), handleOrderListPrintBill/Kitchen()
  - `OrderPage.tsx`: printKitchenOrders(), handlePrintBill()
  - `SalesPage.tsx`: handleOrderListPrintBill(), handleOrderListPrintKitchen()

If edits are required, they must be done in a separate branch and reviewed, or run with `ALLOW_MOVE_MERGE_EDITS=1` for one-off commits (not recommended).

**QSR 모드 수정 시:** 반드시 사용자에게 "QSR 모드의 [기능명]을 수정해도 될까요?" 라고 물어보고 승인을 받은 후에만 수정할 것.

**프린터 출력 수정 시:** 반드시 사용자에게 "프린터 출력의 [기능명]을 수정해도 될까요?" 라고 물어보고 승인을 받은 후에만 수정할 것.

- **Table Map Manager (완성됨 - 2026-02-04):**
  - `frontend/src/pages/TableMapManagerPage.tsx` - 테이블맵 생성/편집 페이지 전체
  - `frontend/src/pages/SalesPage.tsx` - 테이블맵 렌더링 및 표시 (getElementStyle, getElementDisplayName, getElementClass 함수)
  - `backend/routes/table-map.js` - 테이블맵 API 라우트
  - `backend/setup-table-map-db.js` - 테이블맵 DB 스키마
  
  **핵심 기능 (수정 금지):**
  - 테이블 요소 생성 (Square, Circle만 T1, T2 이름 부여)
  - 드래그 & 드롭 이동
  - 리사이즈 및 회전 (텍스트는 수평 유지)
  - 테이블 번호 생성 (timestamp 기반 고유 ID + 순차적 표시 번호)
  - 좌표 스케일링 (백오피스 ↔ SalesPage 일치)
  - Floor 관리 (다중 층 지원)
  - Screen Size 설정
  - Undo/Redo 히스토리
  
  **⚠️ 수정 전 반드시 사용자 컨펌 필요!**

**테이블맵 수정 시:** 반드시 사용자에게 "테이블맵의 [기능명]을 수정해도 될까요?" 라고 물어보고 승인을 받은 후에만 수정할 것.

- **Order Screen DnD / Layout (완성됨 - 2026-03-03):**
  - `frontend/src/components/order/MenuItemGrid.tsx` - 메뉴 아이템 그리드 (DnD, 빈슬롯, EMPTY 토큰, 레이아웃 빌드)
  - `frontend/src/components/order/ModifierPanel.tsx` - 모디파이어 패널 (DnD, 빈슬롯, 레이아웃)
  - `frontend/src/pages/order/OrderCatalogPanel.tsx` - 카탈로그 패널 (onMenuGridReorder, layoutIds 전달)
 
 **핵심 기능:**
 - 드래그 앤 드롭으로 아이템/모디파이어 위치 이동 (삽입+밀어내기 방식)
 - 빈 슬롯(EMPTY 토큰) 생성/유지/렌더링
 - 레이아웃 ID 빌드 (layoutIds, capacity, baseIds)
 - 머지(Mergy) 카테고리 그리드 렌더링
 - 아이템/모디파이어 커스텀 컬러 적용
 - menuItemOrderByCategory / modifierLayoutByItem 저장/로드
 - 카테고리별/아이템별 모디파이어 레이아웃 키 분기 (modLayoutKey, modCategoryKey)
 
 **⚠️ 수정 전 반드시 사용자 컨펌 필요!**

**Order Screen DnD/레이아웃 수정 시:** 반드시 사용자에게 "Order Screen DnD/레이아웃의 [기능명]을 수정해도 될까요?" 라고 물어보고 승인을 받은 후에만 수정할 것.

- **Excel Import/Export (완성됨 - 2026-03-03):**
  - `backend/routes/menu.js` - Export/Import 라우트 (export-excel, import-excel)
  - `pos-desktop/backend/routes/menu.js` - POS Desktop Export/Import 라우트
  - `pos-desktop/routes/menu.js` - POS Desktop 레거시 Export/Import 라우트
  - `frontend/src/pages/MenuEditPage.tsx` - handleExcelExport(), handleExcelImport(), UI 버튼
 
 **핵심 기능 (수정 금지):**
 - Excel Export: 4개 시트 (Menu Date, Modifiers, Taxes, Printers) 생성 및 다운로드
 - Excel Import: 파일 업로드 → 시트 파싱 → DB 트랜잭션 (카테고리/아이템/그룹 생성 → 연결)
 - Import 실행 순서: 그룹 생성 → name→id 맵 구축 → 카테고리/아이템 연결 (순서 변경 금지)
 - EXCEL_COLUMNS 상수, getExcelValue() 헬퍼, LEGACY_COLUMNS 매핑
 
 **⚠️ 수정 전 반드시 사용자 컨펌 필요!**

**Excel Import/Export 수정 시:** 반드시 사용자에게 "Excel Import/Export의 [기능명]을 수정해도 될까요?" 라고 물어보고 승인을 받은 후에만 수정할 것.